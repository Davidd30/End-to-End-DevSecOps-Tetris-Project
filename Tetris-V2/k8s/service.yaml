apiVersion: v1
kind: Service
metadata:
  name: tetris-service
  namespace: tetris
spec:
  type: LoadBalancer
  selector:
    app: tetris
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
